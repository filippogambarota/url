name: Build redirect folders from links.json

on:
  push:
    branches: [ main ]   # or whatever branch you want

jobs:
  build-redirects:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run link builder script
        run: |
          python links.py
          echo "Resulting tree:"
          ls -R

      - name: Commit and push changes (if any)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Add new/changed files
          git add .

          # If no changes, exit gracefully
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          git commit -m "Build redirect folders from links.json"
          git push
